rules:
  type-enum:
    - 2
    - always
    - - feat
      - fix
      - refactor
      - chore
      - docs
      - test
      - perf
      - ci
  type-empty: [2, never]
  subject-empty: [2, never]
  subject-case: [2, never, sentence-case]
  header-max-length: [2, always, 60]
